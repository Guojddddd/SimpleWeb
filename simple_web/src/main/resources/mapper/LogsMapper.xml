<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="simpleweb.mapper.LogsMapper">
    <insert id="save" parameterType="simpleweb.entity.LogsEntity">
        insert into simple_random.logs
        (clob)
        values
        (#{clob})
    </insert>
</mapper>